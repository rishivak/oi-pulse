export default function AlertsPage() {
  return (
    <div className="flex flex-1 flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-xl font-semibold text-terminal-text">Alerts</h1>
      <p className="text-sm text-terminal-muted">
        Coming in Phase 3 — configurable OI spike / PCR threshold alerts with in-app and notification delivery.
      </p>
    </div>
  );
}
